# FM Radio
get_prop(system_app, vendor_fm_prop)
allow vendor_fm_app mediametrics_service:service_manager find;
allow vendor_fm_app vendor_fm_prop:file { getattr map open read };

# Xiaomi Parts
allow system_app sysfs_thermal:file rw_file_perms;

# Allow init to set attribute to sysfs_kprofiles
allow init sysfs_kprofiles:file setattr;
